{
  "name": "@repo/logger",
  "version": "0.1.0",
  "private": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "lint": "eslint src --ext .ts",
    "prepublishOnly": "npm run build",
    "test:test": "node -r ts-node/register src/Test.ts"
  },
  "dependencies": {
    "winston": "^3.13.0"
  },
  "devDependencies": {
    "@repo/eslint-config": "file:../packages/eslint-config",
    "@repo/tsconfig": "file:../packages/tsconfig"
  }
}
